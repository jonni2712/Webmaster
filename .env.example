# ============================================
# WEBMASTER MONITOR - ENVIRONMENT VARIABLES
# ============================================
# Copia questo file come .env.local per sviluppo locale
# Carica queste variabili su Vercel per produzione
# Per Docker self-hosted: cp .env.example .env

# ============================================
# DEPLOYMENT MODE
# ============================================
# cloud   = Usa Supabase Cloud (default, consigliato)
# selfhost = Self-hosted con PostgreSQL locale
DEPLOYMENT_MODE=cloud

# ============================================
# DOCKER SELF-HOSTING (solo se DEPLOYMENT_MODE=selfhost)
# ============================================
POSTGRES_USER=webmaster
POSTGRES_PASSWORD=cambia-questa-password-sicura
POSTGRES_DB=webmaster
POSTGRES_PORT=5432

# Database URL per self-hosting (auto-generato in Docker)
# DATABASE_URL=postgresql://webmaster:password@db:5432/webmaster

# ============================================
# SUPABASE
# ============================================
# URL del progetto Supabase (Settings > API > Project URL)
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxxx.supabase.co

# Chiave pubblica anonima (Settings > API > anon public)
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.xxxxxxxxxxxxx

# Chiave service role - SEGRETA! (Settings > API > service_role secret)
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.xxxxxxxxxxxxx

# ============================================
# NEXTAUTH.JS
# ============================================
# URL base dell'applicazione
# Sviluppo: http://localhost:3000
# Produzione: https://tuo-dominio.vercel.app
# Docker: http://localhost:3000 o https://tuo-dominio.com
NEXTAUTH_URL=https://tuo-dominio.vercel.app
NEXT_PUBLIC_APP_URL=https://tuo-dominio.vercel.app

# Chiave segreta per cifrare i token JWT (minimo 32 caratteri)
# Genera con: openssl rand -base64 32
NEXTAUTH_SECRET=genera-una-chiave-segreta-di-almeno-32-caratteri

# ============================================
# OAUTH PROVIDERS (opzionale)
# ============================================
# GitHub OAuth (https://github.com/settings/developers)
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Google OAuth (https://console.cloud.google.com/apis/credentials)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# ============================================
# EMAIL — SMTP (preferito) o Resend (fallback)
# ============================================
# Il client email prova prima SMTP, poi Resend, poi logga in console.
#
# --- SMTP (consigliato) ---
# Compatibile con qualsiasi provider SMTP standard: cPanel, Brevo,
# Amazon SES, Mailgun, Postmark, Gmail App Password, ecc.
# Per cPanel: SMTP_HOST = mail.tuodominio.com
#             SMTP_PORT = 465 (SSL) o 587 (STARTTLS)
#             SMTP_USER = l'indirizzo email completo (es. noreply@tuodominio.com)
#             SMTP_PASS = la password della casella
#             SMTP_SECURE = true per porta 465, false per 587
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=false

# --- Resend (fallback, opzionale) ---
# Usato solo se SMTP_HOST non è impostato.
# API Key da https://resend.com/api-keys
RESEND_API_KEY=

# Indirizzo mittente (deve matchare un dominio verificato sul provider scelto)
EMAIL_FROM=Webmaster Monitor <noreply@tuodominio.com>

# ============================================
# CRON JOBS
# ============================================
# Chiave segreta per autenticare le chiamate cron
# Genera con: openssl rand -base64 32
CRON_SECRET=genera-una-chiave-segreta-per-cron

# ============================================
# APIS ESTERNE
# ============================================
# Google PageSpeed Insights API (per Core Web Vitals)
# https://developers.google.com/speed/docs/insights/v5/get-started
PAGESPEED_API_KEY=

# ============================================
# SICUREZZA
# ============================================
# Chiave per cifrare API keys e secrets nel database (AES-256-GCM)
# OBBLIGATORIA in produzione. Senza questa variabile, le chiamate
# che cifrano/decifrano API key falliranno.
#
# Genera una chiave hex da 32 bytes con:
#   openssl rand -hex 32
#
# IMPORTANTE: NON modificare questa chiave dopo aver cifrato dati esistenti,
# altrimenti tutti i valori cifrati diventeranno illeggibili.
# Per la rotazione, serve uno script di re-encryption.
ENCRYPTION_KEY=

# ============================================
# OPZIONALE - CANALI NOTIFICA
# ============================================
# Slack Webhook URL
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/xxx/xxx/xxx

# Telegram Bot Token
# TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz

# Discord Webhook URL
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/xxx/xxx

# ============================================
# DOCKER PRODUCTION (opzionale)
# ============================================
# Per deployment con Traefik e SSL automatico
# DOMAIN=webmaster.tuodominio.com
# LETSENCRYPT_EMAIL=admin@tuodominio.com
# APP_PORT=3000
# STUDIO_PORT=3001

# ============================================
# SENTRY ERROR MONITORING
# ============================================
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_DSN=
SENTRY_AUTH_TOKEN=

# ============================================
# Stripe Billing
# ============================================
STRIPE_SECRET_KEY=sk_test_...
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
